<header class="lg:px-16 px-6 bg-red-700 flex flex-wrap items-center lg:py-0 py-3">
    <div class="max-w-5xl m-auto flex w-full">
      <div class="flex-1 flex justify-between items-center text-3xl  duration-500">
        <div class="hover:text-white transition-colors"><%= link_to "RTIX", root_path %> <i class="fas fa-ticket-alt"></i></div>
        <% if current_user%>
        <div class="mx-4 text-sm mr-auto hover:text-white"><%= link_to "我的票卷", dashboards_path(current_user.id) %> </div>
        <% else %>
        <div class="mx-4 text-sm mr-auto hover:text-white"><%= link_to('我的票卷', new_session_path(:user) ) %></div>
        <% end %>
      </div>
      
         <label for="menu-toggle" class="pointer-cursor lg:hidden block"><svg class="fill-current text-gray-900" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 20 20"><title>menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"></path></svg></label>
        <input class="hidden" type="checkbox" id="menu-toggle" />
      
      
        <div class="hidden lg:flex lg:items-center lg:w-auto w-full" id="menu">
        <%= form_with url:search_events_path, local: true, method: :get, html: { class:'search_bar flex' } do |form|%>
            <%= form.text_field :q, data: { behavior: "autocomplete" }, class: "search_input"%>
            <%= form.button '', class: "search_button" do%>
              <i class="fas fa-search text-2xl"></i>
            <% end %>
        <% end %>
      <nav>
        <ul class="lg:flex items-center justify-between text-base text-white pt-4 lg:pt-0 mx-3 ">
      
          <li>
          <div class="mx-3">
          <% if current_user %>
          <%= current_user.email %>
          <%= link_to '登出', destroy_user_session_path, data:{method: 'delete'}, class:"user-signout" %> |
          <%= link_to('修改密碼', edit_registration_path(:user)) %>
          </div>
          </li>
      
          
            <% if current_user.organizations.count == 0 %>
            <li><%= link_to '建立活動', new_organization_path, data: { confirm: '請先建立組織，才能創辦活動' } %></li>
            <% else %>
            <li data-controller="header">
              <div class="org_choice_button font-bold cursor-pointer" id="org_choice_button">選擇組織</div>
              <div class="org_dropdown_list" id="org_dropdown_list">
                <% current_user.organizations.each do |organization|%>
                  <div class="bg-gray-100 px-1 rounded-lg text-center mb-1.5"><%= link_to "#{organization.title}", events_organization_path(organization.id), class: "text-black text-xs hover:bg-gray-900 hover:text-white font-bold rounded-lg px-2"%></div>
                <% end %>
              </div>
            </li>
            <% end %>
          
      
        <% else %>
          <li><%= link_to('註冊', new_registration_path(:user), class: "lg:p-4 py-3 px-0 block border-b-2 border-transparent hover:border-indigo-400") %></li>
          <li><%= link_to('登入', new_session_path(:user), class: "lg:p-4 py-3 px-0 block border-b-2 border-transparent hover:border-indigo-400") %></li>
          <% end %>
          </ul>
        </nav>
        </div>
    </div>
</header>