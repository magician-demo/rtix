<nav class="org-sidebar">
  <div class="org-list">
    <%= link_to '組織公開頁面', "/organizations/#{params[:id]}", target: "_blank" %>
    <%= link_to '活動列表', events_organization_path %>
    <%= link_to '組織帳務', appropriations_organization_path %>
    <h2 class="title w-full bg-black hover:bg-black text-2xl text-white text-center p-2 ">組織設定</h2>
    <%= link_to '一般資訊', info_organization_path %>
    <a href="#/business_infos">帳務設定</a>
    <%= link_to '繳費記錄', orders_organization_path %>
  </div>
  <%= link_to '刪除組織', organization_path(@organization),
                            method: :delete,
                            data: { confirm: '確定要刪除組織嗎？' },
                            class: "delete-button" %>
  <div class="h-screen bg-gray-700 w-full" aria-hidden="true"></div>
</nav>
<label class="sidebar-button">
  ＞
  <input type="checkbox" class="side-btn hidden"></input>
</label>

<script>
  document.addEventListener('turbolink:load', () => {
    const btn = document.querySelector('.side-btn')
    const sidebar = document.querySelector('.org-sidebar')
    btn.addEventListener('click', () => {
      console.log(123);
      sidebar.classList.toggle('toggle')
    })
  })
</script>
