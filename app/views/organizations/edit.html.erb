<div class="h-screen pt-6">
  
  <div class="h-9/10 flex">
    <%= render 'sidebar' %>
    
      <div class="w-full">
        <div class="w-full text-xl mt-5 text-gray-500 border-b border-gray-400 px-4">
          組織名稱 <span class="inline-block ml-3 text-3xl text-red-600"><%= @organization.title %></span>
        </div>
        <container class="w-full px-4">
          <h1 class="p-4 text-2xl">編輯組織</h1>
          <div id="notice"><%= notice %></div>
           <%= render 'form', organization: @organization %>
          <%= link_to '取消', @organization, class: "org-button" %>
        </container>
      </div>
  </div>
  <%= render 'footer' %>
</div>